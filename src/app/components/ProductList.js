import ProductItems from "./ProductItem";
import objectArr from "../products";

function ProductList() {
  const productList = objectArr.map((object) => <></>);
  return <div className="cookie-list">{cookiesList}</div>;
}

export default productItem;
